#include <bits/stdc++.h>
using namespace std;int main(){
    ios::sync_with_stdio(false);cin.tie(nullptr);
    char a,b; if(!(cin.get(a))) return 0; if(!(cin.get(b))) b='\0';
    cout<<(a==b?'1':'0');
    return 0;
}
